import 'package:flutter/material.dart';

class NearbyStation extends StatelessWidget {
  const NearbyStation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
